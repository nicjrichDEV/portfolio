---
const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="font-gs">
  {
    date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
